# Security Checklist

- Rotate Firebase Admin keys and never commit service account files.
- Restrict CORS to production domains only.
- Enforce Firestore security rules before launch.
- Add audit logs for achievement minting and recruiter postings.
- Add bot protection on public form endpoints.
- Enable dependency scanning in CI and GitHub Advanced Security.
- Use Polygon multisig owner for production mint contract.

