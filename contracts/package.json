{
  "name": "@campus-bandhu/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "hardhat node",
    "build": "hardhat compile",
    "lint": "echo 'No contract lint configured yet'",
    "typecheck": "echo 'Contract type safety validated during hardhat compile'",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network amoy",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/node": "^22.15.18",
    "dotenv": "^16.5.0",
    "hardhat": "^2.24.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  }
}
